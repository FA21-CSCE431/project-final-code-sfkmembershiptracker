<h1 class="text-6xl font-black">Our Team</h1>
<hr class="mb-10">

<!--
<div id= "team" class "min-w-full">
<table>
    <tr>
        <th> Name </th>
        <th> Graduation Date </th>
        <th> Role </th>
    </tr>
    <% @team.each do |teammate| %>
    <tr>
        <td><%= teammate.full_name%></td>
        <td><%= teammate.grad_date%></td>
        <td><%= image_tag(teammate.user.avatar_url) %></td>
    </tr>
    <% end %>
</div>
-->


<div class="w-full bg-gray-100 px-10 pt-10">
  <div class="container mx-auto">
    <div role="list" aria-label="Behind the scenes People " class="lg:flex md:flex sm:flex items-center xl:justify-between flex-wrap md:justify-around sm:justify-around lg:justify-around">
      <% @team.each do |teammate| %>
        <div role="listitem" class="xl:w-1/3 sm:w-3/4 md:w-2/5 relative mt-16 mb-32 sm:mb-24 xl:max-w-sm lg:w-2/5">
          <div class="rounded overflow-hidden shadow-md bg-white">
            <div class="absolute -mt-20 w-full flex justify-center">
              <div class="h-32 w-32">
                <!--<img src="https://cdn.tuk.dev/assets/photo-1564061170517-d3907caa96ea.jfif" alt="Display Picture of Andres Berlin" role="img" class="rounded-full object-cover h-full w-full shadow-md" />-->
                <%= image_tag(teammate.user.avatar_url, :class => "rounded-full object-cover h-full w-full shadow-md") %>
              </div>
            </div>
            <div class="px-6 mt-16">
              <h1 class="font-bold text-3xl text-center mb-1"><%= teammate.full_name%></h1>
              <p class="text-gray-800 text-sm text-center"><%= teammate.position.role%></p>
              <p class="text-center text-gray-600 text-base pt-3 font-normal"><%= teammate.bio%></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
