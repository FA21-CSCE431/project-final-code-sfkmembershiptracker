<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Edit Member Profile</h1>

  <%= render "form", member: @member %>

  <% if current_user.member.position.can_change_roster? %>
    <%= link_to "Member Roster", members_path, class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
  <% end %>
</div>
